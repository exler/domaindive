services:
    web:
        build: .
        ports:
            - "4000:3000"
        volumes:
            - ./domaindive.db:/app/domaindive.db
        environment:
            - NODE_ENV=production
        restart: unless-stopped
